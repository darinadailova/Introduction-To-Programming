#include<iostream>
#include<vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<string>data;
    string input;
    char letter;
    cin >> letter;
    
    for (int i = 0; i < n; i++) {
        cin >> input;
        data.push_back(input);
    }
    for (int i = 0; i < n; i++) {
        
    }
   

    return 0;
}
